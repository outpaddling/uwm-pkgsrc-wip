# $NetBSD$
#
###########################################################
#               Generated by fbsd2pkg                     #
###########################################################

DISTNAME=	spikedetekt2-${PORTVERSION}
PKGNAME=	${PYPKGPREFIX}-spikedetekt2-${PORTVERSION}
CATEGORIES=	science python
MASTER_SITES=	${MASTER_SITE_GITHUB:=klusta-team/}
GITHUB_PROJECT=	spikedetekt2
GITHUB_TAG=	v${PORTVERSION}

MAINTAINER=	bacon@NetBSD.org
HOMEPAGE=	https://github.com/klusta-team/spikedetekt2
COMMENT=	Detecting and extracting spikes from raw recordings
# LICENSE=

# Should probably be klustakwik-0.3.0, but doesn't work with 0.3.0.rc2,
# which is an illegal cersion number.
DEPENDS=	klustakwik:../../uwm-pkgsrc-wip/klustakwik030

PYDISTUTILSPKG=	yes

PORTVERSION=	0.3.0

# Verify that we shouldn't use egg.mk or extensions.mk instead.
.include "../../lang/python/distutils.mk"
.include "../../mk/bsd.pkg.mk"
