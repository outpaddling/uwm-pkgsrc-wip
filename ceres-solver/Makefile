# $NetBSD$
#
###########################################################
#                  Generated by fbsd2pkg                  #
#              Thu Sep  8 08:56:40 CDT 2016               #
###########################################################

###########################################################
# Unconverted and partially converted FreeBSD port syntax:

## Created by: Jason Bacon <bacon@NetBSD.org>
## $FreeBSD$
## Deps: http://ceres-solver.org/building.html
## eigen 3.2.2+, cmake 2.8.0+, Google Log 0.3.1+, Google Flags, SuiteSparse,
## CXSparse (alternative to SuiteSparse), BLAS, LAPACK (OpenBLAS without threading)
# BUILD_DEPENDS=	${LOCALBASE}/libdata/pkgconfig/eigen3.pc:math/eigen3
# LIB_DEPENDS=	libglog.so:devel/glog \
#		libgflags.so:devel/gflags \
#		libumfpack.so:math/suitesparse
# Note: cmake
#.include <bsd.port.mk>

DISTNAME=	ceres-solver-${PORTVERSION}
CATEGORIES=	math
MASTER_SITES=	http://ceres-solver.org/

MAINTAINER=	dsiercks@uwm.edu
HOMEPAGE=	http://ceres-solver.org/
COMMENT=	Library for modeling and solving complicated optimization problems
# LICENSE=	${WRKSRC}/LICENSE

# Test and change if necessary.
# MAKE_JOBS_SAFE=	no

# Just assuming C and C++: Adjust this!
USE_LANGUAGES=	c c++
USE_CMAKE=	yes

PORTVERSION=	1.11.0
DATADIR=	${PREFIX}/share/ceres-solver
DOCSDIR=	${PREFIX}/share/doc/ceres-solver
EXAMPLESDIR=	${PREFIX}/share/examples/ceres-solver

# Sets OPSYS, OS_VERSION, MACHINE_ARCH, etc..
# .include "../../mk/bsd.prefs.mk"

# Keep this if there are user-selectable options.
# .include "options.mk"

# Specify which directories to create before install.
# You should only need this if using your own install target.
INSTALLATION_DIRS=	bin lib ${PKGMANDIR}/man1 share/doc share/examples

# BUILD_DEPENDS=        ${LOCALBASE}/libdata/pkgconfig/eigen3.pc:math/eigen3
# LIB_DEPENDS=  libglog.so:devel/glog \
#               libgflags.so:devel/gflags \
#               libumfpack.so:math/suitesparse
.include "../../math/eigen3/buildlink3.mk"
.include "../../devel/google-glog/buildlink3.mk"
.include "../../wip/gflags/buildlink3.mk"
.include "../../wip/suitesparse/buildlink3.mk"
# Convert any _DEPENDS above that have a buildlink3.mk
# .include "../..///buildlink3.mk"
# Linux doesn't have zlib in the base, so just in case...
.include "../../devel/zlib/buildlink3.mk"
.include "../../mk/bsd.pkg.mk"
