# $NetBSD$

###########################################################
#               Generated by fbsd2pkg                     #
###########################################################

###########################################################
# Unconverted and partially converted FreeBSD port syntax:

# # Hack to allow variable length arrays, which are not supported by ISO C++
# USE_GCC=	yes
# SHEBANG_FILES=	src/tophat2.in

# http://ccb.jhu.edu/software/tophat/downloads/tophat-2.0.12.tar.gz

DISTNAME=	tophat-2.0.12
CATEGORIES=	biology
MASTER_SITES=	http://ccb.jhu.edu/software/tophat/downloads/

MAINTAINER=	bacon4000@gmail.com

COMMENT=	A spliced read mapper for RNA-Seq
# LICENSE=	

# Pessimistic assumption.  Test and change if possible.
MAKE_JOBS_SAFE=	no

# Just assuming C and C++: Adjust this!
USE_LANGUAGES=	c c++
GNU_CONFIGURE=	yes
# FreeBSD's SHEBANG_FILES may include bash, perl, python, etc.
# I don't know which is which, so you'll have to finish.
# Add bash, etc. to USE_TOOLS if used below.
# 
REPLACE_SH=	
REPLACE_BASH=	
REPLACE_CSH=	
REPLACE_KSH=	
REPLACE_PERL=	
REPLACE_PYTHON=	

CFLAGS+=	-Wno-unused
# These are getting left out on CentOS in spite of buildlink
LDFLAGS+=	-lboost_thread -lboost_system -lboost_serialization

# Guess based on FreeBSD USE_PYTHON_RUN=yes
.include "../../lang/python/application.mk"
.include "../../wip/samtools/buildlink3.mk"
.include "../../devel/boost-libs/buildlink3.mk"
# CentOS can't find libbam without this.  samtools buildlink issue?
.include "../../devel/zlib/buildlink3.mk"
.include "../../mk/bsd.pkg.mk"
