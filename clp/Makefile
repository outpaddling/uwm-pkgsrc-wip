# $NetBSD$
#
###########################################################
#                  Generated by fbsd2pkg                  #
#              Wed Nov  2 11:51:27 CDT 2016               #
###########################################################

###########################################################
# Unconverted and partially converted FreeBSD port syntax:

## Created by: Robin Schilham
## $FreeBSD: head/math/clp/Makefile 413746 2016-04-21 16:43:14Z swills $
#BROKEN_powerpc64=	Does not build
# Unknown tool: USE_TOOLS=	blaslapack
# Unknown tool: USE_TOOLS=	fortran
# Unknown tool: USE_TOOLS=	pathfix
#USE_LDCONFIG=	yes
#TEST_TARGET=	unitTest
#TEST_WRKSRC=	${WRKSRC}/Clp
#		--with-blas-lib="${BLASLIB} -L${PREFIX}/lib" \
#		--datadir=${DATADIR}
#OPTIONS_DEFINE=	DEBUG
#PORTDOCS=	*
#DEBUG_CXXFLAGS=	-fomit-frame-pointer
#post-install:
#	${STRIP_CMD} ${DESTDIR}${PREFIX}/lib/*.so
#.include <bsd.port.mk>

DISTNAME=	Clp-${PORTVERSION}
CATEGORIES=	math
MASTER_SITES=	http://www.coin-or.org/download/source/Clp/ \
		http://www.mavetju.org/download/adopted/
EXTRACT_SUFX=	.tgz

MAINTAINER=	dsiercks@uwm.edu
HOMEPAGE=	http://www.coin-or.org/Clp/index.html
COMMENT=	Linear Programming Solver
# LICENSE=	EPL

# Test and change if necessary.
# MAKE_JOBS_SAFE=	no

# Just assuming C and C++: Adjust this!
USE_LANGUAGES=	c c++ fortran
USE_LIBTOOL=	yes
GNU_CONFIGURE=	yes

CONFIGURE_ENV+=	${MAKE_ENV}

CONFIGURE_ARGS=	--with-lapack-lib="${LAPACKLIB} -L${PREFIX}/lib" \

CXXFLAGS+=	-ffast-math

PORTVERSION=	1.16.8
DATADIR=	${PREFIX}/share/Clp
DOCSDIR=	${PREFIX}/share/doc/Clp
EXAMPLESDIR=	${PREFIX}/share/examples/Clp

# Sets OPSYS, OS_VERSION, MACHINE_ARCH, etc..
# .include "../../mk/bsd.prefs.mk"

# Keep this if there are user-selectable options.
# .include "options.mk"

# Specify which directories to create before install.
# You should only need this if using your own install target.
INSTALLATION_DIRS=	bin include lib ${PKGMANDIR}/man1 share/doc share/examples

# Linux doesn't have zlib in the base, so just in case...
# .include "../../devel/zlib/buildlink3.mk"
.include "../../mk/bsd.pkg.mk"
