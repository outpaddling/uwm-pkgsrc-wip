# $NetBSD$
#
###########################################################
#                  Generated by fbsd2pkg                  #
#              Fri Jul 21 09:41:16 CDT 2017               #
###########################################################

###########################################################

DISTNAME=	JAGS-${PORTVERSION}

#PKGNAME=	JAGS-${PORTVERSION}
#DIST_SUBDIR=	mcmc-jags
CATEGORIES=	math
# FreeBSD MASTER_SITES: SF/mcmc-jags/JAGS/${S}.x/Source/:1
#MASTER_SITES=	${MASTER_SITE_SOURCEFORGE:=mcmc-jags/}
MASTER_SITES=	http://downloads.sourceforge.net/project/mcmc-jags/JAGS/

#DISTFILES=	JAGS-4.3.0.tar.gz

MAINTAINER=	dsiercks@uwm.edu
HOMEPAGE=	http://mcmc-jags.sourceforge.net/
COMMENT=	Just Another Gibbs Sampler
# Check this
LICENSE=	gnu-gpl-v2

# Test and change if necessary.
# MAKE_JOBS_SAFE=	no

# Just assuming C and C++: Adjust this!
USE_LANGUAGES=	c c++ fortran
USE_TOOLS+=	bison gmake pax
USE_LIBTOOL=	yes
GNU_CONFIGURE=	yes

WRKSRC=		${WRKDIR}/JAGS-${PORTVERSION}

CONFIGURE_ARGS=	--with-lapack="-llapack" --with-blas="-lblas"

PORTVERSION=	4.3.0
#DATADIR=	${PREFIX}/share/mcmc-jags
#DOCSDIR=	${PREFIX}/share/doc/mcmc-jags
#EXAMPLESDIR=	${PREFIX}/share/examples/mcmc-jags

# Sets OPSYS, OS_VERSION, MACHINE_ARCH, etc..
# .include "../../mk/bsd.prefs.mk"

# Keep this if there are user-selectable options.
# .include "options.mk"

# Specify which directories to create before install.
# You should only need this if using your own install target.
INSTALLATION_DIRS=	bin include lib ${PKGMANDIR}/man1 share/doc share/examples

# Convert any _DEPENDS above that have a buildlink3.mk
.include "../../math/blas/buildlink3.mk"
.include "../../math/lapack/buildlink3.mk"
# Linux doesn't have zlib in the base, so just in case...
# .include "../../devel/zlib/buildlink3.mk"
.include "../../mk/bsd.pkg.mk"
